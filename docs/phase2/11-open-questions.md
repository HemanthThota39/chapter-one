# Chapter One — Phase 2 Open Questions

> Living list. Resolve each before or during implementation. New questions land here as we go.

## Resolved this round (Round 4)

- ✅ Licence: **MIT** (confirmed)
- ✅ Tagline: **"It all starts with Chapter One."** (confirmed)
- ✅ Data region: **Central India** (confirmed, verified SKU on deploy)

## Confirmations pending from Hemanth

| # | Question | Default (if we ship without input) |
|---|---|---|
| 4 | Domain: register `chapterone.app` / `chapter-one.xyz` / skip for now? | Skip at launch; use Azure default subdomains |
| 5 | Watermark wording on PDF footer: exact copy? | `Generated by Chapter One · chapterone.app/{username}/reports/{slug} · {date}` |
| 6 | First analysis limit per new user during onboarding — any? | No limit |
| 7 | Should the debate sidebar be open by default when viewing your own report? | Default closed on desktop, closed on mobile |
| 8 | Should we show the "Debate" button on private reports too? | Yes — user can debate their own |

## Architectural questions to resolve before detailed design

| # | Question | Notes |
|---|---|---|
| A | Exact region of the existing `testingclaudecode.cognitiveservices.azure.com` Foundry resource? | Determines cross-region latency budget |
| B | Should `dev` and `prod` share one Azure Entra External ID tenant or be separate? | Default: separate (clean user boundary) |
| C | Postgres: use `pgvector` now (for future semantic retrieval in debates) or wait until needed? | Default: wait until Phase 3 |
| D | Blob public access: use signed URLs or public containers for avatars/PDFs? | Default: public container for avatars (read-only, no PII); signed URLs for PDFs |
| E | SSE timeout tuning: how long can an SSE connection stay open on Container Apps? | Research: Container Apps ingress default 4-min idle, must heartbeat |
| F | Rate limit on Entra External ID (free tier)? | Check quotas; 50K MAU free, auth throughput limits? |
| G | Do we expose a raw API (`/api/*`) to the frontend only, or support Bearer tokens for future CLI? | Default: cookie-only for frontend, Bearer behind feature flag for later |

## UX questions to resolve in Round 4

| # | Question |
|---|---|
| UX1 | What does the landing page look like for a logged-out user? |
| UX2 | First-time onboarding: single page (username + avatar + display name) or multi-step wizard? |
| UX3 | Submit-idea flow: single modal on feed or dedicated `/new` page? |
| UX4 | Progress page: own route (`/analyses/{id}`) or stays on the feed as a card in progress? |
| UX5 | Debate panel default state on different screen sizes |
| UX6 | Empty states for feed, profile, notifications, history |
| UX7 | Dark mode: support or not at launch? |
| UX8 | Error states: analysis failed partway — can user retry just the failed agents? |
| UX9 | PDF download: spinner in the download button or show a toast? |
| UX10 | Notification granularity: do we aggregate "5 people fired your post" or show individually? |

## Data model questions

| # | Question |
|---|---|
| DM1 | Do we dedupe identical ideas across users or allow duplicates? (Currently: allow.) |
| DM2 | What counts as a "completed analysis" for streak purposes — status=done regardless of verdict? |
| DM3 | Streak timezone — cross-DST behavior? |
| DM4 | Can a user share a specific version of a report, or always the latest? |
| DM5 | If owner deletes a public report, what happens to debate transcripts that other users contributed to? |

## Security / privacy questions

| # | Question |
|---|---|
| S1 | Should we log idea text in any observability output? Default: **no** (`LOG_IDEA_TEXT=false` carry over from Phase 1) |
| S2 | PDFs in Blob — expose via signed URL (time-limited) or public direct? Default: signed URL with 1-hour TTL |
| S3 | Backup encryption: use Microsoft-managed keys (default) or customer-managed (overkill for MVP)? |
| S4 | Log retention window for App Insights — cost vs debugging. Default: 30 days |

## Questions to answer during implementation (not blocking design)

- Analyses cost accounting per user — how granular?
- Should the frontend cache feed locally (service worker)?
- On accept-patch, should we snapshot the full report or just the changed sections?
- Slug format if title is non-English (Hindi etc.) — transliterate or hash?
